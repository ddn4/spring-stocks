kind: ConfigMap
apiVersion: v1
metadata:
  name: stock-service
  namespace: spring-stocks
data:
  application.yaml: |-
    spring:

      stocks:
        seeder:
          enabled: true

    server:
      port: 8080
