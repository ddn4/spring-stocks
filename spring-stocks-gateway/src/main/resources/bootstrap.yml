spring:
  application:
    name: spring-stocks-gateway

---
spring:
  profiles: kubernetes

  cloud:

    kubernetes:
      config:
        name: spring-stocks-gateway
        namespace: spring-stocks